import type { Theme } from "../types";

export const feraxDefault: Theme = {
  id: "ferax-default",
  name: "Ferax Default",
  description: "The default Ferax look -- clean glass over neutral surfaces.",
  editorTheme: { dark: "atomone", light: "atomone" },
  variants: {
    light: {},
    dark: {},
  },
};